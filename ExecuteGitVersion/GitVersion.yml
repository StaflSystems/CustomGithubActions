mode: Mainline
update-build-number: true
commit-message-incrementing: Enabled
branches:
  main:
    regex: ^main$
    increment: None
    is-mainline: true
  dev:
    regex: ^dev
    increment: None
    is-mainline: false
    source-branches: ['main', 'feature', 'bugfix']
  
